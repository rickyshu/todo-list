import styled from "styled-components";

const Nav = () => {
  return (
    <NavBar>
      {
        //map을 사용해서 아이콘들을 정령하는 방식을 사용!
      }
    </NavBar>
  );
};

const NavBar = styled.nav`
  width: 50rem;
  height: 15rem;
  border-radius: 4rem;
  border: 1px solid red;
`;
export default Nav;
